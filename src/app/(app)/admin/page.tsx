import DashboardPage from "../dashboard/page";

// For this scaffold, the Admin dashboard shows the same content as the Vendor dashboard.
// In a real application, this would show platform-wide analytics.
export default function AdminDashboardPage() {
    return <DashboardPage />;
}
